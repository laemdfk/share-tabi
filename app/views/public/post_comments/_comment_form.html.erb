<%= form_with url: [public_post_path, public_post_post_comments_path] do |f| %>
  <%= f.text_area :post_comment, rows: '5', placeholder: "コメント", class: "w-100" %>
  <%= f.submit "コメントを送信",class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>